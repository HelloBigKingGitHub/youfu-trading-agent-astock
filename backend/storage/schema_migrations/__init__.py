"""Schema migrations package — Django-style migration journal for SQLite.

Public entry point: ``backend.storage.schema_migrations.migrate`` (the
``migrate`` function inside ``migrate.py``). This ``__init__`` only marks
the directory as a Python package so the module is importable.
"""