# sector-rotation

Add a sector rotation report feature: use np-ipick (eastmoney hot stock picking) + baidu PAE (reverse lookup concept blocks for hot stocks) to generate a daily A-stock sector rotation digest usable both as an independent deliverable and as input to the hot_money_tracker analyst.
